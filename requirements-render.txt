# Dependencies for deployment on Render.
#
# Keeps memory footprint inside Render's 512 MB free tier by using the built-in
# NumPy vector store rather than ChromaDB, while providing full support for
# NVIDIA Nemotron models (extraction, verification, embeddings) and LangGraph.

fastapi>=0.115
uvicorn[standard]>=0.34

pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
PyYAML>=6.0
httpx>=0.27
numpy>=1.26

langgraph>=0.2.50
langchain-core>=0.3.20
langchain-nvidia-ai-endpoints>=0.3.5
