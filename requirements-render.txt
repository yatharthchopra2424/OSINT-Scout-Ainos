# Dependencies for the public demo on Render.
#
# Smaller than requirements.txt on purpose. The demo runs the deterministic control
# path only, so it never imports chromadb (the vector store falls back to the
# built-in NumPy one) or langchain-nvidia-ai-endpoints (no model calls). Leaving
# them out keeps the build fast and the memory footprint inside the free tier's
# 512 MB.

fastapi>=0.115
uvicorn[standard]>=0.34

pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
PyYAML>=6.0
httpx>=0.27
numpy>=1.26

langgraph>=0.2.50
langchain-core>=0.3.20
