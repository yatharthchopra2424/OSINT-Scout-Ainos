# OSINT Scout — dependencies
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)   |   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Floors, not exact pins: this has to install on whatever Python you have.

# --- web layer ---------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.34

# --- config / data -----------------------------------------------------------
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
PyYAML>=6.0
httpx>=0.27
numpy>=1.26

# --- agent + RAG -------------------------------------------------------------
langgraph>=0.2.50
langchain-core>=0.3.20
langchain-nvidia-ai-endpoints>=0.3.5

# Optional. The app falls back to its built-in numpy vector store if this is
# missing, so a failed chromadb build does not stop you working.
chromadb>=0.5.20
