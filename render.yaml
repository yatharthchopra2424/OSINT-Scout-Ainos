# Render Blueprint: deploys OSINT Scout.
#
# In the Render dashboard: New > Blueprint > select this repository.

services:
  - type: web
    name: osint-scout
    runtime: python
    plan: free
    region: singapore
    branch: main
    buildCommand: pip install --upgrade pip && pip install -r requirements-render.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: DEMO_MODE
        value: "false"
      - key: SOURCE_MODE
        value: fixture
      - key: EMBED_BACKEND
        value: lexical
      - key: VECTOR_BACKEND
        value: numpy
      - key: NVIDIA_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
